# 치이카와 파인더

치이카와 관련 정보를 인물 · 온라인 · 오프라인 · 굿즈 네 갈래로 모아 보여 주는 정적 사이트입니다.
아이보리 배경에 채도를 낮춘 파스텔 톤을 쓰고, 웹과 모바일 모두 대응합니다.

## 실행

```bash
pnpm install
pnpm dev      # 개발 서버
pnpm build    # out/ 에 정적 사이트 생성
pnpm start    # 생성된 정적 사이트 서빙
pnpm check    # 스크래퍼 로직 테스트
pnpm scrape   # 치이카와 마켓 신상 굿즈 갱신
```

## 구조

| 경로 | 내용 | 규모 |
|---|---|---|
| `src/data/characters.json` | 캐릭터 프로필과 등장 에피소드 | 45명 · 에피소드 581건 |
| `src/data/online.json` | 만화 · 애니 · 나가노 작가 작품을 볼 수 있는 경로와 링크 | 72건 |
| `src/data/places.json` | 한국 · 일본의 팝업, 매장, 카페, 콜라보 식당 (위경도 포함) | 50곳 |
| `src/data/goods.json` | 최근 6개월 굿즈. `cm-` 접두사는 스크래퍼가 채우고 나머지는 수동 | 2,007건 |
| `src/data/groupbuy-channels.json` | 공구가 열리는 플랫폼과 결제 보호 수준 | 5곳 |
| `src/data/fx.json` | 엔/원 환율. 매일 갱신 | |
| `src/types/` | 네 데이터셋의 도메인 타입 | |
| `scripts/scrape.mjs` | 치이카와 마켓 신상 수집 | |
| `scripts/fx.mjs` | 엔/원 환율 갱신 | |
| `scripts/download-images.mjs` | 인물·장소 이미지를 `public/images` 로 내려받고 JSON을 로컬 경로로 교체 | |
| `scripts/merge-characters.mjs` | 인물 데이터 두 갈래를 slug 기준으로 합침 | |
| `scripts/sources.md` | 확인된 기계 판독 엔드포인트 정리 | |

## 이미지

인물과 장소 이미지는 `pnpm images` 로 파일을 내려받아 `public/images` 에 두고 로컬 경로로 참조합니다.
핫링크는 차단이나 CDN 만료로 조용히 깨지기 때문입니다. 굿즈는 2천 건이라 Shopify CDN을 그대로 씁니다.

인물 45명 중 37명에 사진이 있습니다. 나머지는 이름만 나오는 단역·괴물이라 공식 이미지도 굿즈도 없어
흰 배경에 파스텔 원으로 표시합니다.

## 굿즈 자동 갱신

`scripts/scrape.mjs` 가 치이카와 마켓의 Shopify `products.json` 을 읽어 최근 6개월 상품을 채웁니다.
발매일은 `published_at` 이 아니라 상품 태그의 8자리 날짜(`20260925`, `PRE20260513`, `RE20260601`)에서 뽑습니다.
`published_at` 은 상품이 다시 공개될 때 갱신되어 신뢰할 수 없기 때문입니다.

재입고(`RE20260601`)는 새 발매가 아니므로 발매일로 세지 않고 이력으로만 모읍니다.
재고(`variants[].available`)도 함께 가져옵니다. 치이카와 마켓은 상품의 79%가 품절 상태로 남아 있어,
이 값이 없으면 살 수 없는 상품을 구매 링크로 안내하게 됩니다.

`cm-` 접두사가 붙은 항목만 스크래퍼가 덮어쓰고, 손으로 넣은 국내 굿즈는 그대로 둡니다.
`.github/workflows/scrape.yml` 이 매일 오전 6시(KST)에 돌면서 변경분을 커밋합니다.

## 지도

Leaflet + OpenStreetMap 타일을 씁니다. API 키가 필요 없습니다.
기본 타일은 채도가 높아 `.leaflet-tile-pane` 에 CSS 필터를 걸어 파스텔 톤에 맞췄습니다.

## 홈 화면 3D

`src/components/hero/` 가 치이카와·하치와레·우사기를 three.js 기본 도형으로 조립해 움직인다.
외부 모델 파일 없이 구와 캡슐만 쓴다.

- `faceTexture.ts` — 얼굴을 텍스처로 그려 머리에 입힌다. 눈·눈썹·볼을 3D 조각으로 붙이면
  표면에서 떠 보인다. 구의 UV는 위아래로 갈수록 가로가 좁아지므로 좌표마다 그 왜곡을 되돌린다.
  배치는 공식 인형 사진에서 잰 비율을 그대로 옮겼다.
  눈은 짙은 고리 안에 흰자가 차고 그 아래 짙은 초승달이 하나 더 있다. 흰 덩어리를 둘로 나누면 눈이 갈라져 보인다.
  눈썹은 캐릭터마다 다르다. 치이카와는 짧게, 하치와레는 점처럼, 우사기는 이마를 가로지르는 긴 활이다.
  하치와레의 파란 머리도 텍스처로 그린다. 도형 두 개를 겹치면 면이 맞닿아 지글거린다.
- `characters.ts` — 도형 조립. 비율이 핵심이라 머리가 전체 높이의 6할을 차지하고 몸은 그 아래 작게 붙는다.
  귀는 축을 밑동에 둬야 흔들려도 머리에서 떨어져 보이지 않는다.
  재질은 `sheen` 을 준 물리 재질이다. 일반 재질은 아무리 거칠게 해도 플라스틱처럼 보인다.
  톤매핑은 쓰지 않는다. 밝은 색을 눌러 흰 인형이 회색이 된다.
- `motions.ts` — 캐릭터별 시그니처 동작. three.js 에 의존하지 않는 순수 함수라 값만 놓고 검증한다.
  치이카와는 떨다가 울고, 하치와레는 가볍게 폴짝거리고, 우사기는 크게 뛰며 몸을 튼다.
  뒤통수에는 얼굴이 없으므로 회전은 제한한다.
- `HeroSceneInner.tsx` — 렌더링. three.js 는 홈에 들어왔을 때만 내려받고, 탭이 가려지면 그리기를 멈춘다.
  `prefers-reduced-motion` 을 켠 사람에게는 정지 자세를 보여준다.
  주소에 `?still` 을 붙이면 정지 정면으로 그린다. 참조 사진과 대조할 때 쓴다.

## 도구

**발매 · 재입고 캘린더** (`/calendar`) — 일본 신상은 발매일이 상품 태그에만 일본어로 붙어 있고,
공식몰은 재입고 목록을 재입고 전날에 숨긴다. 둘 다 날짜별로 묶어 보여준다.
커뮤니티 조사에서 품절·재입고가 가장 잦은 불편이었다 (`docs/user-needs.md`).

**직구 계산기** (`/calculator`) — 상품을 담으면 최종 얼마인지 계산한다. 다른 곳에 없는 건 두 가지다.

- **공구 환율에 섞인 수수료.** 공구 모집글은 수수료를 따로 적지 않고 "엔당 9.5원" 같은 적용 환율에 넣는다.
  실환율과 나란히 두면 그 차액이 원 단위로 드러난다.
- **면세 한도.** 일본발은 물품가격 150달러를 넘으면 초과분만이 아니라 전체 금액에 관세와 부가세가 붙는다.
  한도 판정에는 국제운임을 빼고 보지만, 실제 과세가격에는 국제운임이 들어간다. 이 비대칭을 지켜 계산한다.
  계산 근거와 출처는 `docs/import-cost.md` 에 있다.

## 수익화

조사 결과는 `docs/monetization.md`, 지표 설계는 `docs/metrics.md` 에 있습니다.

**제휴 링크.** 쿠팡 파트너스, 네이버 쇼핑커넥트, Buyee(Indoleads)는 기존 링크에 코드를 붙이는 방식이 아니라
각 대시보드에서 상품마다 링크를 따로 발급합니다. 그래서 굿즈 데이터에 `affiliateUrl` 필드를 두고,
발급받은 주소를 그대로 넣습니다. 값이 있으면 구매 버튼이 그 주소로 나가고 제휴 고지가 뜹니다.
치이카와 마켓은 제휴 프로그램이 없어, 지금 굿즈 대부분은 그대로는 수익이 나지 않습니다.

**공동구매.** 개별 주최자는 싣지 않고 공구가 열리는 플랫폼과 결제 보호 수준만 싣습니다.
치이카와 할로윈 공구로 사기 계정이 활동 중이라, 검증 없이 개인 공구를 게시하면 송금을 유도하는 통로가 됩니다.
결제와 배송에 관여하지 않으므로 통관·상표권 문제도 생기지 않습니다.
